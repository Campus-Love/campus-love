import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Details = ({params}) => {
    console.log(params)
    return (
        <View>
            <Text>am details</Text>
        </View>
    )
}

export default Details

const styles = StyleSheet.create({})
